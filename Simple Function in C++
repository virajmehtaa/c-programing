#include<iostream>
using namespace std;

void addition(int x, int y)
{
    int sum = x + y;
    cout << "\nThe Sum is: " << sum;
}

int main()
{
    int a, b;
    cout << "Enter two numbers: ";
    cin >> a >> b;


    addition(a, b);

    return 0;
}
